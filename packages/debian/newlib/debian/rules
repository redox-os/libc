#!/usr/bin/make -f
TARGET=x86_64-elf-redox

%:
	mkdir -p build
	dh $@ --builddirectory=build

override_dh_auto_configure:
	cd build && ../../../../newlib/configure \
	  --target="$(TARGET)" \
	  --prefix=/usr
